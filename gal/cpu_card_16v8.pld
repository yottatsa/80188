GAL16V8    ;
glue_r01v02;


Clock	RESOUT	A3	WR	RD	MIO	SERIAL	CARDSEL	DEN	GND
/OE	BEN	RESET	SMEMW	SMEMR	IOW	IOR	CSS1	NRESET	VCC


RESET = RESOUT

IOR   = RESET +  MIO + RD + DEN
IOW   = RESET +  MIO + WR + DEN
SMEMR = RESET + /MIO + RD + DEN
SMEMW = RESET + /MIO + WR + DEN 

BEN   = RESET + RD * WR + CARDSEL * SERIAL

CSS1  = SERIAL +  A3

NRESET.T = VCC
NRESET.E = GND


DESCRIPTION
- pins 12, 19 can't be used as input, so the NRESET logic is disabled
